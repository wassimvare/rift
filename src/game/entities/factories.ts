import type{BallEntity,PlayerEntity}from'../../types/game.js';import{ARENA}from'../config.js';export function createPlayer(x:number,y:number,color:string,ai=false):PlayerEntity{return{x,y,vx:0,vy:0,r:32,color,ai,face:ai?Math.PI:0,dashCd:0,dashActive:0,perfectConsumed:false,pulseCd:0,polarity:1,flux:0,trail:[]}}export function createBall():BallEntity{return{x:ARENA.width/2,y:ARENA.height/2,vx:0,vy:0,r:25,wallStuck:0}}
