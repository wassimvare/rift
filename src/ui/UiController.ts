import{CATALOG,itemById}from'../data/catalog.js';import{MODES}from'../data/modes.js';import type{InputAction}from'../input/InputManager.js';import type{PlayerState}from'../types/state.js';import type{ModeId}from'../types/game.js';import{qs,qsa}from'./dom.js';export interface UiActions{play():void;pause():void;restart():void;quit():void;dash():void;pulse():void;polarity():void;burst():void;buy(id:string):void;selectMode(mode:ModeId):void;setting<K extends keyof PlayerState['settings']>(key:K,value:PlayerState['settings'][K]):void;captureBinding(action:InputAction):void;navigate(view:string):boolean}export class UiController{private toastTimer=0;private combatTimer=0;constructor(private readonly actions:UiActions){this.bind()}render(state:PlayerState,captureAction:InputAction|null=null):void{qs('#credits').textContent=state.credits.toLocaleString('fr-FR');qs('#shards').textContent=String(state.shards);qs('#statMatches').textContent=String(state.stats.matches);qs('#statWins').textContent=String(state.stats.wins);qs('#statGoals').textContent=String(state.stats.goalsFor);qs('#statCollection').textContent=String(state.inventory.length);qsa<HTMLElement>('.mode').forEach(button=>button.classList.toggle('sel',button.dataset.mode===state.selectedMode));qs('#vaultGrid').innerHTML=state.inventory.map(entry=>{const item=itemById(entry.id);return item?this.card(item):''}).join('')||'<p>Vault vide.</p>';qs('#marketGrid').innerHTML=CATALOG.map(item=>this.card(item,`<button class="ghost buy" data-id="${item.id}">ACHETER</button>`)).join('');qsa<HTMLButtonElement>('.buy').forEach(button=>button.onclick=()=>this.actions.buy(button.dataset.id??''));qs('#historyList').innerHTML=state.matchHistory.length?state.matchHistory.map(entry=>`<article class="panel historyRow"><div><b>${entry.outcome==='win'?'VICTOIRE':entry.outcome==='loss'?'DÉFAITE':entry.outcome==='abandoned'?'ABANDON':'NUL'} ${entry.scoreA}-${entry.scoreB}</b><br><small>${MODES[entry.mode as ModeId]?.name??entry.mode}${entry.overtime?' · OVERTIME':''}</small></div><small>+${entry.credits||0} NC</small></article>`).join(''):'<p>Aucun match terminé.</p>';this.renderSettings(state,captureAction);this.applyTouchSettings(state)}setScore(a:number,b:number):void{qs('#sa').textContent=String(a);qs('#sb').textContent=String(b)}setModeAndClock(mode:string,clock:string):void{qs('#modeTitle').textContent=mode;qs('#clock').textContent=clock}setClock(clock:string):void{qs('#clock').textContent=clock}setPauseLabel(label:string):void{qs('#pauseBtn').textContent=label}setPolarity(pull:boolean):void{const color=pull?'#ff71c6':'#59f5ff',label=pull?'PULL':'PUSH';qs<HTMLElement>('#polarityText').textContent=label;qs<HTMLElement>('#polarityText').style.color=color;qs<HTMLElement>('#pol').style.color=color}abilityHud(dashPct:number,pulsePct:number,pulseText:string,flux:number):void{qs<HTMLElement>('#dashM').style.width=`${dashPct}%`;qs<HTMLElement>('#pulseM').style.width=`${pulsePct}%`;qs('#pulseT').textContent=pulseText;qs<HTMLElement>('#fluxM').style.width=`${flux}%`;qs('#fluxT').textContent=flux>=100?'READY':`${Math.round(flux)}%`;qs('#burst').classList.toggle('abilityReady',flux>=100)}overlay(html:string,visible=true):void{const element=qs('#ov');element.innerHTML=html;element.classList.toggle('hidden',!visible)}combat(text:string,color='#fff',duration=400):void{const e=qs<HTMLElement>('#combatHint');e.textContent=text;e.style.color=color;e.classList.add('show');window.clearTimeout(this.combatTimer);this.combatTimer=window.setTimeout(()=>e.classList.remove('show'),duration)}toast(text:string):void{const e=qs('#toast');e.textContent=text;e.classList.add('show');window.clearTimeout(this.toastTimer);this.toastTimer=window.setTimeout(()=>e.classList.remove('show'),1600)}showModal(html:string):void{qs('#modalContent').innerHTML=html;qs('#modal').classList.remove('hidden')}closeModal():void{qs('#modal').classList.add('hidden')}gameView():void{qsa('.view').forEach(v=>v.classList.remove('active'));qs('#game').classList.add('active');qsa('[data-v]').forEach(b=>b.classList.remove('active'));document.body.classList.add('game');window.scrollTo(0,0)}menuView():void{document.body.classList.remove('game');qsa('.view').forEach(v=>v.classList.remove('active'));qs('#home').classList.add('active');qsa<HTMLElement>('[data-v]').forEach(b=>b.classList.toggle('active',b.dataset.v==='home'))}navigate(view:string):void{qsa('.view').forEach(v=>v.classList.remove('active'));qs(`#${view}`).classList.add('active');qsa<HTMLElement>('[data-v]').forEach(b=>b.classList.toggle('active',b.dataset.v===view));document.body.classList.toggle('game',view==='game');window.scrollTo(0,0)}bindResult(rematch:()=>void,menu:()=>void):void{qs<HTMLButtonElement>('#rematch').onclick=rematch;qs<HTMLButtonElement>('#resultMenu').onclick=menu}bindConfirm(confirmId:string,confirm:()=>void,cancelId:string,cancel:()=>void):void{qs<HTMLButtonElement>(`#${confirmId}`).onclick=confirm;qs<HTMLButtonElement>(`#${cancelId}`).onclick=cancel}private bind():void{qsa<HTMLButtonElement>('[data-v]').forEach(button=>button.onclick=()=>this.actions.navigate(button.dataset.v??'home'));qsa<HTMLButtonElement>('.mode').forEach(button=>button.onclick=()=>this.actions.selectMode(button.dataset.mode as ModeId));qs<HTMLButtonElement>('#play').onclick=()=>this.actions.play();qs<HTMLButtonElement>('#pauseBtn').onclick=()=>this.actions.pause();qs<HTMLButtonElement>('#restartBtn').onclick=()=>this.actions.restart();qs<HTMLButtonElement>('#exitBtn').onclick=()=>this.actions.quit();for(const[id,action]of[['#dash','dash'],['#pulse','pulse'],['#pol','polarity'],['#burst','burst'],['#polarityText','polarity']]as const)qs<HTMLButtonElement>(id).onpointerdown=e=>{e.preventDefault();this.actions[action]()};const bindRange=<K extends'joystickSensitivity'|'touchScale'|'touchInset'>(id:string,key:K)=>qs<HTMLInputElement>(id).addEventListener('input',e=>this.actions.setting(key,Number((e.target as HTMLInputElement).value)as PlayerState['settings'][K]));bindRange('#joySens','joystickSensitivity');bindRange('#touchScale','touchScale');bindRange('#touchInset','touchInset');qs<HTMLSelectElement>('#touchLayout').addEventListener('change',e=>this.actions.setting('touchLayout',(e.target as HTMLSelectElement).value as PlayerState['settings']['touchLayout']));qs<HTMLButtonElement>('#hapticsToggle').onclick=()=>this.toggleBool('haptics');qs<HTMLButtonElement>('#shakeToggle').onclick=()=>this.toggleBool('cameraShake');qs<HTMLButtonElement>('#audioToggle').onclick=()=>this.toggleBool('audio');qsa<HTMLButtonElement>('.bindBtn').forEach(button=>button.onclick=()=>this.actions.captureBinding(button.dataset.bind as InputAction))}private toggleBool(key:'haptics'|'cameraShake'|'audio'):void{const button=key==='haptics'?qs('#hapticsToggle'):key==='cameraShake'?qs('#shakeToggle'):qs('#audioToggle');this.actions.setting(key,button.textContent!=='ON')}private card(item:(typeof CATALOG)[number],button=''):string{return`<article class="panel card"><div class="art" style="--a:${item.a};--b:${item.b}"></div><h3>${item.name}</h3><small>${item.type}</small><div class="price">${item.price.toLocaleString('fr-FR')} NC</div>${button}</article>`}private renderSettings(state:PlayerState,captureAction:InputAction|null):void{const s=state.settings;qs<HTMLInputElement>('#joySens').value=String(s.joystickSensitivity);qs('#joySensValue').textContent=`${Math.round(s.joystickSensitivity*100)}%`;qs<HTMLInputElement>('#touchScale').value=String(s.touchScale);qs('#touchScaleValue').textContent=`${Math.round(s.touchScale*100)}%`;qs<HTMLInputElement>('#touchInset').value=String(s.touchInset);qs('#touchInsetValue').textContent=`${s.touchInset}px`;qs<HTMLSelectElement>('#touchLayout').value=s.touchLayout;qs('#hapticsToggle').textContent=s.haptics?'ON':'OFF';qs('#shakeToggle').textContent=s.cameraShake?'ON':'OFF';qs('#audioToggle').textContent=s.audio?'ON':'OFF';qsa<HTMLButtonElement>('.bindBtn').forEach(b=>{const action=b.dataset.bind as InputAction,label=action.toUpperCase();b.innerHTML=`${label}<br><b>${this.prettyCode(s.keymap[action])}</b>`;b.classList.toggle('waiting',captureAction===action)});const pad=[...(navigator.getGamepads?.()??[])].find(Boolean);qs('#gamepadDot').classList.toggle('on',!!pad);qs('#gamepadStatus').textContent=pad?`Manette détectée : ${pad.id.slice(0,42)}`:'Aucune manette détectée'}private applyTouchSettings(state:PlayerState):void{const arena=qs<HTMLElement>('#arena');arena.style.setProperty('--touch-scale',String(state.settings.touchScale));arena.style.setProperty('--touch-inset',`${state.settings.touchInset}px`);arena.classList.toggle('touch-left',state.settings.touchLayout==='left')}private prettyCode(code:string):string{return({Space:'ESPACE',ArrowUp:'↑',ArrowDown:'↓',ArrowLeft:'←',ArrowRight:'→',Escape:'ÉCHAP'}as Record<string,string>)[code]??code.replace(/^Key/,'').replace(/^Digit/,'')}}
